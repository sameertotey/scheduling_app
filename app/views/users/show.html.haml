%h3 User
%p
  Email: #{@user.email if @user.email}
%p
  Admin: #{@user.admin}
-if current_user.admin  
  %div= link_to "Edit", edit_user_path(@user)
  %div= link_to "Delete", user_path(@user), method: :delete, confirm: 'Are you Sure?'
