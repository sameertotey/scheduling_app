#wrap
  #header-region
  #main-region.container-fluid
  #push
#footer-region

-if current_user
  -content_for :hide_navigation do
    alert('The backbone app will provide navigation')
  :javascript 
    $(function(){
      SchedulingApp.start({
        currentUser: gon.current_user,
        environment: gon.environment,
        eventTypes: gon.event_types
      })
    })
-else
  %h1 Welcome to the Scheduling Application
  %h3 Please sign in
